# 🔗 발행 일정 자동 연결 구조

## 📊 전체 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                    volumeSchedule.js                        │
│                   (단일 데이터 소스)                          │
│                                                             │
│  export const VOLUME_SCHEDULE = {                          │
│    553: { date: "2026-02-05", status: "published" },      │
│    554: { date: "2026-02-20", status: "planned" }         │
│  };                                                         │
│                                                             │
│  export const getCurrentVolume() { ... }                   │
│  export const getContractDates() { ... }                   │
│  export const getVolumeRangeDates() { ... }               │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   │ import
                   │
        ┌──────────┼──────────┬──────────────┐
        │          │          │              │
        ↓          ↓          ↓              ↓
┌───────────┐ ┌──────────┐ ┌─────────┐ ┌──────────┐
│contract   │ │Customer  │ │Volume   │ │  App.js  │
│Status.js  │ │Card.js   │ │Schedule │ │          │
│           │ │          │ │.js      │ │          │
│CURRENT_   │ │계약 날짜  │ │일정 UI  │ │대시보드  │
│VOLUME     │ │표시      │ │        │ │통계      │
│(자동 계산)│ │(자동 계산)│ │(자동)   │ │(자동)    │
└───────────┘ └──────────┘ └─────────┘ └──────────┘
```

---

## 🔄 코드 연결 흐름

### 1단계: volumeSchedule.js (데이터 원본)

```javascript
// src/utils/volumeSchedule.js

// ✅ 여기만 수정하면 됨!
export const VOLUME_SCHEDULE = {
  553: { date: "2026-02-05", status: "published" },
  554: { date: "2026-02-20", status: "planned" },  // ← 수정 위치
  555: { date: "2026-03-05", status: "planned" },
};

// 현재 볼륨 자동 계산 (오늘 날짜 기준)
export const getCurrentVolume = () => {
  const today = new Date();
  // VOLUME_SCHEDULE을 읽어서 자동 계산
  let currentVol = null;
  Object.entries(VOLUME_SCHEDULE).forEach(([vol, info]) => {
    const volDate = new Date(info.date);
    if (volDate <= today && info.status === "published") {
      currentVol = parseInt(vol);
    }
  });
  return currentVol || 553;
};
```

---

### 2단계: contractStatus.js (자동 import)

```javascript
// src/utils/contractStatus.js

import { getCurrentVolume as getAutoVolume } from './volumeSchedule';
//       ↑ volumeSchedule.js에서 자동으로 가져옴

// ✅ 자동으로 현재 볼륨 계산됨 (수동 수정 불필요!)
export const CURRENT_VOLUME = getAutoVolume();

// 계약 상태 판정 (자동으로 최신 CURRENT_VOLUME 사용)
export const getContractStatus = (startVol, endVol, currentVol = CURRENT_VOLUME) => {
  // CURRENT_VOLUME이 자동 업데이트되므로
  // 계약 상태도 자동으로 정확하게 판정됨
  if (currentVol >= startVol && currentVol <= endVol) {
    return { status: "ACTIVE", label: "광고 게재 중" };
  }
  // ...
};
```

---

### 3단계: CustomerCard.js (자동 적용)

```javascript
// src/components/CustomerCard.js

import { getContractStatus, getPaymentStatus } from "../utils/contractStatus";
//       ↑ 자동으로 최신 CURRENT_VOLUME 사용

import { 
  getContractDates,      // 호수 → 날짜 변환
  getVolumeRangeDates,   // 호수별 날짜 목록
  formatDateSimple       // 날짜 포맷
} from "../utils/volumeSchedule";
//    ↑ 자동으로 최신 VOLUME_SCHEDULE 사용

const CustomerCard = ({ customer, onClose }) => {
  const { startVol, endVol } = parseVolumeRange(customer[9]);
  
  // ✅ 자동으로 최신 일정으로 계약 날짜 계산
  const contractDates = getContractDates(startVol, endVol);
  // 결과: { 
  //   startDate: 2026-01-20, 
  //   endDate: 2026-04-02,
  //   duration: 73일
  // }
  
  // ✅ 자동으로 최신 일정으로 호수별 발행일 가져오기
  const volumeDates = getVolumeRangeDates(startVol, endVol);
  // 결과: [
  //   { volume: 552, date: 2026-01-20, status: "published" },
  //   { volume: 553, date: 2026-02-05, status: "published" },
  //   { volume: 554, date: 2026-02-20, status: "planned" },
  //   ...
  // ]
  
  // ✅ 자동으로 최신 CURRENT_VOLUME으로 계약 상태 판정
  const contractStatus = getContractStatus(startVol, endVol);
  // volumeSchedule.js만 수정하면 자동으로 정확한 상태 반환
  
  return (
    <div>
      {/* 계약 날짜 표시 (자동 업데이트) */}
      <div>시작 날짜: {formatDateSimple(contractDates.startDate)}</div>
      <div>종료 날짜: {formatDateSimple(contractDates.endDate)}</div>
      
      {/* 호수별 발행일 표시 (자동 업데이트) */}
      {volumeDates.map(volInfo => (
        <div>
          Vol {volInfo.volume}: {volInfo.dateString}
          {volInfo.status === "published" && "✓"}
        </div>
      ))}
    </div>
  );
};
```

---

### 4단계: VolumeSchedule.js (자동 표시)

```javascript
// src/components/VolumeSchedule.js

import {
  VOLUME_SCHEDULE,      // 일정 데이터 직접 참조
  getCurrentVolume,     // 현재 볼륨 자동 계산
  getNextVolume,        // 다음 볼륨 자동 계산
  formatDateSimple      // 날짜 포맷
} from "../utils/volumeSchedule";
//    ↑ 모두 volumeSchedule.js에서 가져옴

const VolumeSchedule = () => {
  // ✅ 자동으로 현재 볼륨 계산
  const currentVol = getCurrentVolume();
  // volumeSchedule.js의 VOLUME_SCHEDULE 읽어서 자동 계산
  
  // ✅ 자동으로 다음 발행 예정 계산
  const nextVol = getNextVolume(currentVol);
  // 결과: { volume: 554, date: 2026-02-20, daysUntil: 4 }
  
  // ✅ VOLUME_SCHEDULE 직접 참조하여 월별 일정 표시
  const monthlyVolumes = Object.entries(VOLUME_SCHEDULE)
    .filter(([vol, info]) => {
      const date = new Date(info.date);
      return date.getMonth() + 1 === selectedMonth;
    });
  
  return (
    <div>
      {/* 현재 발행 중 (자동 업데이트) */}
      <div>현재: Vol {currentVol}</div>
      
      {/* 다음 발행 예정 (자동 업데이트) */}
      <div>다음: Vol {nextVol.volume} ({nextVol.daysUntil}일 후)</div>
      
      {/* 월별 일정 (자동 업데이트) */}
      {monthlyVolumes.map(([vol, info]) => (
        <div>
          Vol {vol}: {formatDateSimple(info.date)}
          {info.status === "published" && "✓"}
        </div>
      ))}
    </div>
  );
};
```

---

### 5단계: App.js (대시보드 통계)

```javascript
// src/App.js

import { getContractStatus, getPaymentStatus } from "./utils/contractStatus";
//       ↑ 자동으로 최신 CURRENT_VOLUME 사용

const App = () => {
  const [activeAds, setActiveAds] = useState([]);
  
  useEffect(() => {
    // 고객 데이터 로드
    fetchSheetData();
  }, []);
  
  // ✅ 자동으로 최신 볼륨 기준으로 통계 계산
  const activeCount = activeAds.filter(ad => {
    const { startVol, endVol } = parseVolumeRange(ad[9]);
    const status = getContractStatus(startVol, endVol);
    return status.status === "ACTIVE";
  }).length;
  // volumeSchedule.js만 수정하면 activeCount 자동 업데이트
  
  const expiredCount = activeAds.filter(ad => {
    const { startVol, endVol } = parseVolumeRange(ad[9]);
    const status = getContractStatus(startVol, endVol);
    return status.status === "EXPIRED";
  }).length;
  // 자동으로 정확한 만료 고객 수 계산
  
  return (
    <div>
      {/* 통계 카드 (자동 업데이트) */}
      <div>광고 게재 중: {activeCount}</div>
      <div>계약 만료: {expiredCount}</div>
      
      {/* 고객 테이블 (자동 업데이트) */}
      {activeAds.map(ad => {
        const { startVol, endVol } = parseVolumeRange(ad[9]);
        const status = getContractStatus(startVol, endVol);
        // 자동으로 최신 상태 표시
        return (
          <tr style={{ backgroundColor: status.color }}>
            <td>{status.label}</td>
          </tr>
        );
      })}
    </div>
  );
};
```

---

## 🎯 업데이트 시나리오

### 시나리오: Vol 554 발행 (2026.02.20)

#### 1단계: volumeSchedule.js 수정
```javascript
// ⚡ 이것만 수정!
554: { date: "2026-02-20", status: "published" }, // planned → published
```

#### 2단계: 앱 재시작
```bash
npm start
```

#### 3단계: 자동 적용 (코드 수정 불필요!)

##### contractStatus.js
```javascript
// 자동으로 계산됨 (코드 수정 없음)
CURRENT_VOLUME = getCurrentVolume() // 553 → 554
```

##### CustomerCard.js (Awesome Academy, Vol 552~557)
```javascript
// 자동으로 업데이트됨 (코드 수정 없음)
const contractStatus = getContractStatus(552, 557);
// 이전: { status: "ACTIVE", description: "5개 호 남음" }
// 현재: { status: "ACTIVE", description: "4개 호 남음" }

const contractDates = getContractDates(552, 557);
// 계속 같은 함수 호출, 자동으로 최신 일정 사용

const volumeDates = getVolumeRangeDates(552, 557);
// 자동으로 Vol 554의 status가 "published"로 표시됨
```

##### VolumeSchedule.js
```javascript
// 자동으로 업데이트됨 (코드 수정 없음)
const currentVol = getCurrentVolume(); // 553 → 554
const nextVol = getNextVolume(); 
// 이전: { volume: 554, daysUntil: 4 }
// 현재: { volume: 555, daysUntil: 13 }
```

##### App.js
```javascript
// 자동으로 재계산됨 (코드 수정 없음)
const activeCount = activeAds.filter(ad => {
  const status = getContractStatus(startVol, endVol);
  return status.status === "ACTIVE";
}).length;
// Vol 554 종료 고객들이 자동으로 "EXPIRED"로 변경
// activeCount 자동 감소
```

---

## 🔍 import 체인 추적

```
[사용자 수정]
volumeSchedule.js
  ↓ export VOLUME_SCHEDULE
  ↓ export getCurrentVolume()
  ↓ export getContractDates()
  ↓ export getVolumeRangeDates()
  
[자동 import 1]
contractStatus.js
  ↓ import { getCurrentVolume }
  ↓ CURRENT_VOLUME = getCurrentVolume()
  ↓ export CURRENT_VOLUME
  ↓ export getContractStatus()
  
[자동 import 2]
CustomerCard.js
  ↓ import { getContractStatus } from contractStatus
  ↓ import { getContractDates } from volumeSchedule
  ↓ const status = getContractStatus(...)
  ↓ const dates = getContractDates(...)
  
[자동 import 3]
VolumeSchedule.js
  ↓ import { VOLUME_SCHEDULE, getCurrentVolume } from volumeSchedule
  ↓ const currentVol = getCurrentVolume()
  ↓ Object.entries(VOLUME_SCHEDULE).map(...)
  
[자동 import 4]
App.js
  ↓ import { getContractStatus } from contractStatus
  ↓ const status = getContractStatus(...)
  ↓ 대시보드 통계 자동 업데이트
```

---

## 📦 파일 의존성 맵

```
volumeSchedule.js (레벨 0 - 의존성 없음)
       ↓
contractStatus.js (레벨 1)
       ↓
┌──────┴──────┬──────────────┬────────┐
│             │              │        │
CustomerCard  VolumeSchedule  App.js  AddCustomerForm
(레벨 2)      (레벨 2)       (레벨 2) (레벨 2)
```

**핵심: volumeSchedule.js를 수정하면 아래 레벨 모두 자동 업데이트!**

---

## ✅ 검증 방법

### 수정 후 확인 체크리스트

```bash
# 1. volumeSchedule.js 수정
554: { date: "2026-02-20", status: "published" }

# 2. 앱 재시작
npm start

# 3. 브라우저 확인
```

#### ✅ 대시보드
- [ ] 현재 Vol: 554로 표시
- [ ] "광고 게재 중" 수: 자동 감소
- [ ] "계약 만료" 수: 자동 증가
- [ ] 테이블 배경색: 자동 변경

#### ✅ 발행 일정 탭
- [ ] 현재 발행 중: Vol 554
- [ ] 다음 발행 예정: Vol 555
- [ ] 2월 일정: Vol 554에 ✓ 표시

#### ✅ 고객 카드 (Vol 549~554 고객)
- [ ] 계약 상태: "계약 만료"
- [ ] 종료 날짜: 2026.02.20
- [ ] Vol 554: ✓ 표시

#### ✅ 고객 카드 (Vol 552~557 고객)
- [ ] 계약 상태: "광고 게재 중"
- [ ] 남은 호수: 4개 호
- [ ] Vol 554: ✓ 표시 (발행됨)

---

## 🚫 수정하면 안 되는 파일들

### ❌ 직접 수정 금지
```
contractStatus.js
  ✗ CURRENT_VOLUME 직접 변경 금지
  → getCurrentVolume()이 자동 계산함

CustomerCard.js
  ✗ 날짜 하드코딩 금지
  → getContractDates()가 자동 계산함

App.js
  ✗ 계약 상태 직접 입력 금지
  → getContractStatus()가 자동 판정함

VolumeSchedule.js
  ✗ 현재 볼륨 하드코딩 금지
  → getCurrentVolume()이 자동 계산함
```

### ✅ 유일한 수정 지점
```
volumeSchedule.js
  ✓ VOLUME_SCHEDULE 객체만 수정
  ✓ status: "planned" → "published"
  ✓ 새 호수 추가
```

---

## 💡 핵심 원칙

### Single Source of Truth (단일 진실의 원천)
```
volumeSchedule.js = 유일한 데이터 원본
       ↓
모든 파일이 이것을 참조
       ↓
데이터 일관성 보장
```

### Don't Repeat Yourself (반복 제거)
```
날짜 계산 로직이 1곳에만 존재
       ↓
수정 시 1곳만 변경
       ↓
버그 가능성 최소화
```

### Automatic Propagation (자동 전파)
```
데이터 변경 (volumeSchedule.js)
       ↓
import 체인을 통해 자동 전파
       ↓
모든 컴포넌트 자동 업데이트
```

---

**🎯 결론: `volumeSchedule.js` 파일 하나만 수정하면, import 체인을 통해 모든 파일에 자동으로 전파됩니다!**
