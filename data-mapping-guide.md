# 데이터 매핑 가이드
## ADVERTISEMENT DETAILS → 고객관리 시트 변환

### 원본 파일 (ADVERTISEMENT DETAILS_2026xls)
**컬럼 구조:**
- No. (A)
- CUSTOMER (B)
- Address (C)
- Tel (D)
- Page No. (E)
- Size (F)
- Price (G)
- Received (H)
- Have collect (I)
- Remarks (J)

### 목표 파일 (고객관리 시트)
**컬럼 구조:**
1. 고객 유형
2. 업체명
3. 업종
4. 주소
5. 대표자
6. 담당자
7. 이메일
8. 전화번호
9. 모바일
10. 카카오톡
11. 광고 사이즈
12. 지면 계약 총액
13. 시작 호수
14. 종료 호수
15. 단가
16. 온라인 병행 단가
17. 온라인 기간
18. 온라인 총액
19. 시작일
20. 종료일
21. 총 계약 금액
22. 온라인 포함 호당 단가
23-47. Vol 550 ~ Vol 574 (25개 컬럼)
48. 계산서 발행
49. 누적 미수금
50. 최종 상담 기록
51. 추가 상담

---

## 📋 데이터 매핑 규칙

### 직접 매핑 가능 필드

| 원본 (ADVERTISEMENT DETAILS) | 목표 (고객관리 시트) | 비고 |
|------------------------------|---------------------|------|
| CUSTOMER (B) | 업체명 (2번 컬럼) | 그대로 복사 |
| Address (C) | 주소 (4번 컬럼) | 그대로 복사 |
| Tel (D) | 전화번호 (8번 컬럼) | 여러 번호가 있으면 첫 번째는 전화번호, 두 번째는 모바일로 분리 |
| Size (F) | 광고 사이즈 (11번 컬럼) | FC, 1/2, 1/4 등 |
| Price (G) | 단가 (15번 컬럼) | USD 단위 |
| Received (H) | 누적 미수금 계산에 사용 | Price - Received = 미수금 |
| Have collect (I) | 누적 미수금 (49번 컬럼) | 직접 매핑 |
| Remarks (J) | 최종 상담 기록 (50번 컬럼) | 호수 정보 및 기타 메모 |

### 데이터 가공 필요 필드

#### 1. **전화번호 분리**
원본 Tel 필드에 여러 번호가 세미콜론(;)이나 슬래시(/)로 구분되어 있는 경우:
```javascript
const phones = telString.split(/[;\/]/);
전화번호 = phones[0]?.trim();
모바일 = phones[1]?.trim();
```

**예시:**
- `028 5417 1885; 0966 950 499` → 전화번호: `028 5417 1885`, 모바일: `0966 950 499`

#### 2. **고객 유형 판단**
Size 필드 기준으로 분류:
```javascript
if (Size === "FC") return "대형광고주";
if (Size === "1/2" || Size === "1/2.") return "중형광고주";
if (Size === "1/4" || Size === "1/4.") return "소형광고주";
if (Size === "yellowpage") return "옐로우페이지";
if (Size === "fleamarket") return "프리마켓";
```

#### 3. **계약 호수 파싱**
Remarks 필드에서 호수 정보 추출:
```javascript
// 예: "552~557" → 시작: 552, 종료: 557
// 예: "549~560" → 시작: 549, 종료: 560
// 예: "monthly~Jul'26" → 월간 계약

const parseVolumeRange = (remarks) => {
  const match = remarks.match(/(\d+)~(\d+)/);
  if (match) {
    return { 시작호수: match[1], 종료호수: match[2] };
  }
  return { 시작호수: "", 종료호수: "" };
}
```

#### 4. **Vol 550 ~ Vol 574 개별 컬럼 채우기**
계약 호수 범위 내에 해당하는 Vol 컬럼에 단가 입력:
```javascript
const fillVolumeColumns = (startVol, endVol, price) => {
  const volumes = {};
  for (let vol = 550; vol <= 574; vol++) {
    if (vol >= startVol && vol <= endVol) {
      volumes[`Vol ${vol}`] = price;
    } else {
      volumes[`Vol ${vol}`] = "";
    }
  }
  return volumes;
}
```

#### 5. **지면 계약 총액 계산**
```javascript
지면계약총액 = 단가 × (종료호수 - 시작호수 + 1)
```

#### 6. **누적 미수금 계산**
```javascript
누적미수금 = Price(G) - Received(H)
// 또는 Have collect(I) 값 직접 사용
```

---

## 🔄 변환 스크립트 예시

```javascript
function transformAdvertisementData(adRows) {
  return adRows.map(row => {
    const [no, customer, address, tel, pageNo, size, price, received, haveCollect, remarks] = row;
    
    // 전화번호 분리
    const phones = tel.split(/[;\/,]/);
    const phone = phones[0]?.trim() || "";
    const mobile = phones[1]?.trim() || "";
    
    // 고객 유형 판단
    let customerType = "";
    if (size === "FC") customerType = "대형광고주";
    else if (size?.includes("1/2")) customerType = "중형광고주";
    else if (size?.includes("1/4")) customerType = "소형광고주";
    else if (size === "yellowpage" || size === "Yellow page") customerType = "옐로우페이지";
    else if (size === "fleamarket") customerType = "프리마켓";
    
    // 호수 범위 파싱
    const volumeMatch = remarks?.match(/(\d+)~(\d+)/);
    const startVol = volumeMatch ? parseInt(volumeMatch[1]) : null;
    const endVol = volumeMatch ? parseInt(volumeMatch[2]) : null;
    
    // 계약 총액 계산
    const totalAmount = startVol && endVol ? 
      parseFloat(price?.replace(/[^0-9.]/g, '')) * (endVol - startVol + 1) : 
      parseFloat(price?.replace(/[^0-9.]/g, '')) || 0;
    
    // Vol 550 ~ 574 컬럼 생성
    const volumeColumns = {};
    for (let vol = 550; vol <= 574; vol++) {
      volumeColumns[`Vol ${vol}`] = 
        (startVol && endVol && vol >= startVol && vol <= endVol) ? 
        price : "";
    }
    
    // 미수금 계산
    const unpaid = parseFloat(haveCollect?.replace(/[^0-9.]/g, '')) || 0;
    
    return {
      "고객 유형": customerType,
      "업체명": customer,
      "업종": "", // 원본에 없음 - 수동 입력 필요
      "주소": address,
      "대표자": "", // 원본에 없음
      "담당자": "", // 원본에 없음
      "이메일": "", // 원본에 없음
      "전화번호": phone,
      "모바일": mobile,
      "카카오톡": "", // 원본에 없음
      "광고 사이즈": size,
      "지면 계약 총액": totalAmount,
      "시작 호수": startVol || "",
      "종료 호수": endVol || "",
      "단가": price,
      "온라인 병행 단가": "", // 원본에 없음
      "온라인 기간": "", // 원본에 없음
      "온라인 총액": "", // 원본에 없음
      "시작일": "", // 원본에 없음
      "종료일": "", // 원본에 없음
      "총 계약 금액": totalAmount,
      "온라인 포함 호당 단가": "", // 원본에 없음
      ...volumeColumns,
      "계산서 발행": "", // 원본에 없음
      "누적 미수금": unpaid,
      "최종 상담 기록": remarks,
      "추가 상담": "" // 원본에 없음
    };
  });
}
```

---

## ⚠️ 주의사항

### 원본에 없는 데이터 (수동 입력 필요)
다음 필드는 원본 파일에 정보가 없어 추가 조사가 필요합니다:
- **업종**: 각 업체의 비즈니스 카테고리
- **대표자**: 대표자 성명
- **담당자**: 영업 담당자 이름
- **이메일**: 이메일 주소
- **카카오톡**: 카카오톡 ID
- **온라인 병행 단가/기간/총액**: 온라인 광고 관련 정보
- **시작일/종료일**: 정확한 날짜 (호수로만 표시됨)
- **계산서 발행**: 세금계산서 발행 여부

### 데이터 정제 필요 항목
1. **전화번호 형식**: 다양한 구분자 (세미콜론, 슬래시, 하이픈) 통일 필요
2. **가격 단위**: `$` 기호 제거 및 숫자 변환
3. **광고 사이즈**: `1/2.`, `1/2`, `FC` 등 표기 통일
4. **Remarks**: 호수 정보 외 추가 메모 분리

---

## 📊 예시 데이터 변환

### 입력 (ADVERTISEMENT DETAILS)
```
No: 1
CUSTOMER: Awesome Academy
Address: 204B/1 Nguyen Van Huong st., An Khanh ward, HCMC
Tel: 093 888 5891
Page No: 16
Size: FC
Price: $450
Received: $450
Have collect: $0
Remarks: 552~557
```

### 출력 (고객관리 시트)
```
고객 유형: 대형광고주
업체명: Awesome Academy
업종: [수동입력필요]
주소: 204B/1 Nguyen Van Huong st., An Khanh ward, HCMC
대표자: [수동입력필요]
담당자: [수동입력필요]
이메일: [수동입력필요]
전화번호: 093 888 5891
모바일: 
카카오톡: [수동입력필요]
광고 사이즈: FC
지면 계약 총액: $2,700
시작 호수: 552
종료 호수: 557
단가: $450
온라인 병행 단가: [수동입력필요]
온라인 기간: [수동입력필요]
온라인 총액: [수동입력필요]
시작일: [수동입력필요]
종료일: [수동입력필요]
총 계약 금액: $2,700
온라인 포함 호당 단가: [수동입력필요]
Vol 550: 
Vol 551: 
Vol 552: $450
Vol 553: $450
Vol 554: $450
Vol 555: $450
Vol 556: $450
Vol 557: $450
Vol 558: 
... (Vol 574까지)
계산서 발행: [수동입력필요]
누적 미수금: $0
최종 상담 기록: 552~557
추가 상담: [수동입력필요]
```

---

## 🚀 다음 단계

1. **자동 변환 스크립트 실행**: 위의 JavaScript 코드를 사용하여 일괄 변환
2. **수동 데이터 보완**: 원본에 없는 필드를 영업팀에서 직접 입력
3. **데이터 검증**: 가격, 호수, 미수금 등 숫자 데이터 확인
4. **CRM 시스템 연동**: App.js에서 두 번째 시트를 읽도록 수정

필요하시면 자동 변환 스크립트를 React 컴포넌트로 작성해드리겠습니다!
